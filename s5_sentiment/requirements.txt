# S5 Sentiment Engine — module-level requirements
# (subset of the global requirements.txt)

# NLP / model inference
transformers>=4.40.0        # FinBERT pipeline (text-classification)
torch>=2.2.0                # GPU inference; CPU fallback supported

# Data
numpy>=1.26.0               # OLS residualization (lstsq)
sqlalchemy>=2.0.0           # ORM; PostgreSQL dialect
psycopg2-binary>=2.9.0      # PostgreSQL driver

# Config & validation
pydantic>=2.0.0             # config model validation (used in shared/)
pyyaml>=6.0.0               # YAML config parsing (used in shared/)

# Logging
structlog>=24.0.0           # structured JSON logging

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
